# Disposable-home recovery acceptance — 2026-09-05

Result: all 11 recovery acceptance checks passed again against candidate 7214af5b6832404b4d758322320efac951275146, built from an ordinary clean clone with Go 1.26.8 and embedded vcs.modified=false. Actual PTY/CLI processes ran on Darwin arm64. Full case list, exact source metadata, and binary SHA256 are in acceptance.json; fresh raw PTY output, rendered terminal screens, and CLI streams are adjacent. The earlier report is preserved as acceptance-0f057da.json; its raw PTY/run/build logs are archived at /tmp/dotfiles-recovery-0f057da-evidence-3tbpmbo_.

The TUI backed up eight synthetic zsh/tmux/Neovim/Ghostty/git/Yazi configs. Cancel preserved modified originals; confirmed restore and CLI restore recovered every byte and POSIX mode. Uninstall restored the latest session and retained every recovery payload. A live destination symlink produced a truthful partial failure and left its target unchanged. Missing backup payload made uninstall fail while preserving the live unrestorable file and available backups. A separate legacy directory manifest restored nested Neovim bytes/modes and removed newly-created descendants and an absent-before directory.

At 40x14, the Backups listing/header are clipped; selected backup details and the restore/delete footer are visible. The full Backups heading returns at 100x32 and 80x24. This is a documented narrow-terminal limitation, not evidence of a fully visible small layout. No GUI-terminal visual, font, mouse or owner-account checks are claimed.

Historical supplemental test evidence from the earlier 0f057da working tree: pinned tests passed for internal/backup, internal/safefile, internal/operation, and cmd/dotfiles. These adjacent test logs were not regenerated by the 7214af5 recovery rerun. An initial unpinned run used PATH Go 1.27.1 and failed 13 doctor-repair tests due to empty static module/command metadata. The same source passes the pinned 1.26.8 tests. go-tests-bounded.log records the initial outcome; its approximately 200 MB binary diagnostic payload is preserved outside the repository at /tmp/dotfiles-recovery-unpinned-tests-full.log. This is a toolchain-compatibility limitation, not a failure of the pinned release build.

The owner install/apply gate in tasks/install-apply-owner-test.md remains open: this task did not have a disposable macOS VM/spare Mac with a demonstrated full-disk snapshot restore. Temporary HOME isolation verifies file-level recovery only and cannot substitute for platform/package rollback. Arch/Pi, authenticated integrations, and physical terminal checks are outside this bounded result. The recovery rerun did not modify product source, the existing acceptance harness, owner configuration, or packages.
