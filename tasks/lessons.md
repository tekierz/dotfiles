# Lessons

- When an audit worktree changes shape during review, stop and re-baseline the
  branch before continuing. Do not keep patching findings from an obsolete diff;
  report status, identify the new changed-file set, and re-plan against the
  current branch state.
