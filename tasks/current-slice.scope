# Parse and validate one immutable restore catalog snapshot without applying any item.
# Authority: complete manifest syntax, source identifiers, targets, kinds, existence, desired
# modes, deterministic ordering, duplicate rejection, and immutable directory snapshot binding.
# Invariants: parsing performs no restore mutation and retains source, target, kind, existence,
# and desired mode for the later opaque BA1C apply join.
# Dependency: closed BA1SF2D at 75efdc0.
# Deferred: source feasibility at apply time, BA1SF2M execution, BA1C opaque public authority,
# and BR3 byte/count limits.
version=2
slice_id=ba1b2-immutable-catalog-kernel
state=reviewed
max_total_files=2
max_production_files=1
max_changed_lines=800
ignore=tasks/pi-agent-integration-spec.md
allow=internal/backup/catalog.go
allow=internal/backup/catalog_restore_test.go
allow=tasks/slice-commit-ledger.tsv
test=internal/backup/catalog_restore_test.go
